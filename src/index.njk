---
layout: base.njk
title: Jonathan Hart - CV
description: "The CV of Jonathan Hart, a software developer with six years experience building innovative web applications for education."
---

<div class="intro">
  <div class="intro-content">
    <div class="intro-avatar">
      <img src="/assets/avatar.jpg" alt="Picture of {{ intro.name }}" class="avatar" width="800" height="800">
    </div>
    <div class="intro-text">
      <h1>{{ intro.name }}</h1>
      <p>{{ intro.statement }}</p>
    </div>
  </div>
</div>

<section class="section">
  <h2 class="section-title">
    <div class="section-title-content">
      Employment History
    </div>
  </h2>
  
  <ol class="timeline" reversed>
    {% for job in work %}
      <li class="timeline-item">
        <div class="timeline-marker"></div>
        <div class="timeline-content">
          <header class="timeline-header">
            <h3 class="timeline-title">{{ job.title }}</h3>
            <div class="timeline-subtitle">{{ job.company }}</div>
            <time class="timeline-date">{{ job.period }}</time>
          </header>
          <div class="timeline-body">
            <p>{{ job.summary }}</p>
            {% if job.details %}
              <ul>
                {% for detail in job.details %}
                  <li>{{ detail }}</li>
                {% endfor %}
              </ul>
            {% endif %}
          </div>
        </div>
      </li>
    {% endfor %}
  </ol>
</section>

<section class="section">
  <h2 class="section-title">
    <div class="section-title-content">
      Skills
    </div>
  </h2>
  
  <div class="card">
    <h3 class="card-title">Frontend Development</h3>
    <ul class="skills-list">
      {% for skill in skills.frontend %}
        <li><span class="pill">{{ skill }}</span></li>
      {% endfor %}
    </ul>
  </div>

  <div class="card">
    <h3 class="card-title">Backend Development</h3>
    <ul class="skills-list">
      {% for skill in skills.backend %}
        <li><span class="pill">{{ skill }}</span></li>
      {% endfor %}
    </ul>
  </div>

  <div class="card">
    <h3 class="card-title">Tools & Technologies</h3>
    <ul class="skills-list">
      {% for skill in skills.tools %}
        <li><span class="pill">{{ skill }}</span></li>
      {% endfor %}
    </ul>
  </div>
</section>

<section class="section">
  <h2 class="section-title">
    <div class="section-title-content">
      Education
    </div>
  </h2>
  
  <ol class="timeline" reversed>
  {% for item in education %}
    <li class="timeline-item">
      <div class="timeline-marker"></div>
      <div class="timeline-content">
        <header class="timeline-header">
          <h3 class="timeline-title">{{ item.title }}</h3>
          <div class="timeline-subtitle">{{ item.institution }}</div>
          <time class="timeline-date">{{ item.period }}</time>
        </header>
      </div>
    </li>
  {% endfor %}
  </ol>
</section>

<section class="section">
  <h2 class="section-title">
    <div class="section-title-content">
      Contact
    </div>
  </h2>
  
  <p class="contact-intro">I'm always interested in new opportunities and collaborations. Feel free to reach out!</p>
  
  <div class="contact-grid">
    <a href="#" class="contact-card email-contact" 
       data-user="contact" 
       data-domain="jonhart.dev">
      <div class="icon-circle">
        {% icon "lucide:mail" %}
      </div>
      <div class="contact-info">
        <h3 class="contact-title">Email</h3>
        <p class="contact-detail email-display">Click to contact</p>
      </div>
    </a>

    <a href="https://www.linkedin.com/in/jonathan-hart-286321137" class="contact-card" target="_blank" rel="noopener noreferrer">
      <div class="icon-circle">
        {% icon "lucide:linkedin" %}
      </div>
      <div class="contact-info">
        <h3 class="contact-title">LinkedIn</h3>
        <p class="contact-detail">linkedin.com/in/jonathan-hart-286321137</p>
      </div>
    </a>

  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const emailContact = document.querySelector('.email-contact');
  const emailDisplay = document.querySelector('.email-display');
  
  if (emailContact && emailDisplay) {
    const user = emailContact.dataset.user;
    const domain = emailContact.dataset.domain;
    const email = `${user}@${domain}`;
    
    // Show email on hover for better UX
    emailContact.addEventListener('mouseenter', function() {
      emailDisplay.textContent = email;
    });
    
    // Revert to "Click to contact" on mouse leave
    emailContact.addEventListener('mouseleave', function() {
      emailDisplay.textContent = 'Click to contact';
    });
    
    // Handle click to open email client
    emailContact.addEventListener('click', function(e) {
      e.preventDefault();
      window.location.href = `mailto:${email}`;
    });
  }
});
</script>
